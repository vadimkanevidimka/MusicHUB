<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:models="clr-namespace:MusicHUB.Models"
             x:Class="MusicHUB.Pages.PlayListsPage"
             Title="Плейлисты">

    <ListView x:Name="MyListView"
            ItemsSource="{Binding Items.Result}"
            ItemTapped="Handle_ItemTapped"
            CachingStrategy="RecycleElement">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:Track">
                <ImageCell ImageSource="{Binding ImageSource}" Text="{Binding Title}" TextColor="Black" Detail="{Binding Artist}"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
