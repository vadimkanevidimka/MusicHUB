<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:customelements="clr-namespace:MusicHUB.CustomElements" 
             xmlns:models="clr-namespace:MusicHUB.Models"
             x:Class="MusicHUB.Pages.TrackLirycsPopupPage">

    <ContentPage.Content>
        <StackLayout VerticalOptions="Fill" Margin="0,-100,0,0" BackgroundColor="Transparent">
            <ScrollView VerticalOptions="Fill" BackgroundColor="Transparent">
                <StackLayout Padding="0,600,0,0" BackgroundColor="Transparent">
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ClosePopupPage}"/>
                    </StackLayout.GestureRecognizers>
                    <Frame CornerRadius="20" Grid.Row="0" VerticalOptions="FillAndExpand" Padding="0" BackgroundColor="{AppThemeBinding Dark=#3c2e0b, Light=Wheat}" HeightRequest="3000">
                        <WebView Source="{Binding Lyrics.Result}" Margin="0,-55,0,0" VerticalOptions="FillAndExpand"/>
                    </Frame>
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>